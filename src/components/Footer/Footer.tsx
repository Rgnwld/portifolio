import React from 'react';
import { FooterContainer, InfoContainer } from './Footer.style';

const Footer: React.FC = () => {
    return (
        <FooterContainer>
            <InfoContainer>
                <strong>2023</strong> / Reginaldo Filho
            </InfoContainer>
        </FooterContainer>
    );
};

export default Footer;
